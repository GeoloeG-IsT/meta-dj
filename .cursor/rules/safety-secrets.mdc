---
description: Prevent secrets and sensitive data leaks
globs: "**/*"
---
- Do not include secrets, tokens, keys, or credentials in code, logs, or docs.
- Use environment variables and secret managers.
- Redact sensitive values in examples and error messages.
- Add `.env*` and known secret patterns to `.cursorignore` and `.gitignore`.